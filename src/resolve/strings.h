#ifndef DASADOS_RESOLVE_STRINGS_H
#define DASADOS_RESOLVE_STRINGS_H

#include <vector>
#include <string>

namespace resolve
{
std::vector<std::string> Split(const std::string& source, const std::string& tag);
}

#endif
